{
  "name": "editbridge",
  "version": "1.0.0",
  "private": true,
  "description": "EditBridge full-stack MVP marketplace for video editors and content creators.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
